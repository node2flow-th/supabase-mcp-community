services:
  supabase-mcp-community:
    build: .
    container_name: supabase-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3030:3000"
    mem_limit: 512m
    mem_reservation: 256m
